{% extends 'base.html.twig' %}

{% block title %}New Team{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Create Team</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_teams_index') }}">
                    View All Teams
                </a>
            </div>
            <div class="card-body">
                {{ include('teams/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}